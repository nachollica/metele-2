.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.env
.env.local

# Local SQLite databases. Production uses Postgres via DATABASE_URL.
*.db
*.sqlite
*.sqlite3

# Generated vector pools (built by the top-level word-assets tool; large, so not
# committed). The image build fails loudly if they are missing.
/data/word_pool/

!.python-version
